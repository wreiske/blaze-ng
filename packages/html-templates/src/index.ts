/**
 * @blaze-ng/html-templates — Meta-package combining core + templating.
 *
 * This is the primary entry point for projects using Blaze-NG with
 * HTML templates. Re-exports everything from:
 * - `@blaze-ng/core` (View, DOMRange, render, Template, builtins, etc.)
 * - `@blaze-ng/templating` (Template registration, body, compiler, etc.)
 */

export * from '@blaze-ng/core';
export * from '@blaze-ng/templating';
